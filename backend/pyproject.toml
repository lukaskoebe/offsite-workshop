[project]
name = "offsite-workshop-api"
version = "0.1.0"
description = "FastAPI backend for the Recipe Book app"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
]
